apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - mongodb.yaml
  - redis.yaml
  - api-gateway.yaml
  - auth-service.yaml
  - turf-service.yaml
  - booking-service.yaml
  - frontend.yaml
  - hpa.yaml

# Create a separate kustomization or use kubectl apply directly for monitoring/logging
# to avoid namespace conflicts caused by the top-level 'namespace: turf-platform'
# - monitoring/monitoring.yaml
# - logging/namespace.yaml
# - logging/loki.yaml
# - logging/promtail.yaml
